| Method | URI | คำอธิบาย | Status Code ที่เป็นไปได้ |
| :--- | :--- | :--- | :--- |
| **GET** | `/api/v1/students` | ดึงรายชื่อนักศึกษาทั้งหมด | 200(ดึงสำเร็จ) |
| **GET** | `/api/v1/students/{id}` | ดึงข้อมูลนักศึกษารายบุคคล (รองรับ `?include=courses`) | 200(ดึงสำเร็จ), 404(ไม่พบข้อมูล) |
| **POST** | `/api/v1/students` | เพิ่มนักศึกษาใหม่ | 201(เพิ่มสำเร็จ), 400(เพิ่มไม่ครบ), 409(มีข้อมูลอยู่แล้ว) |
| **PUT** | `/api/v1/students/{id}` | แก้ไขข้อมูลนักศึกษาทั้งระเบียน | 200(แก้ไขสำเร็จ), 400(ข้อมูลไม่ครบ), 404(ไม่พบข้อมูล) |
| **PATCH** | `/api/v1/students/{id}` | แก้ไขข้อมูลนักศึกษาบางส่วน | 200, 404 |
| **DELETE** | `/api/v1/students/{id}` | ลบนักศึกษา | 200, 404 |